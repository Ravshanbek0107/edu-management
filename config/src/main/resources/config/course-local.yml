server:
  port: ${services.ports.course}
spring:
  application:
    name: course

  datasource:
    url: jdbc:postgresql://localhost:5432/edu_platform
    username: postgres
    password: 1

  jpa:
    properties:
      hibernate:
        default_schema: course

    hibernate:
      ddl-auto: update

    show-sql: true