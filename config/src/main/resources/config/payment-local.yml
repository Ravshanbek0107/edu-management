server:
  port: ${services.ports.payment}
spring:
  application:
    name: payment

  datasource:
    url: jdbc:postgresql://localhost:5432/edu_platform
    username: postgres
    password: 1

  jpa:
    properties:
      hibernate:
        default_schema: payment

    hibernate:
      ddl-auto: update

    show-sql: true